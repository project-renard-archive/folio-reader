#!/usr/bin/env perl

BEGIN {#{{{
	local $ENV{PERL_DL_NONLAZY} = 1;
	#$ENV{PERL_TKX_TRACE} = 64;
	require Tkx;
	local $ENV{PERL_DL_NONLAZY} = 0;
	$|=1; # TODO
}#}}}
use strict;
use FindBin qw($Bin);
use lib "$Bin/../lib";
use Folio::App;

exit Folio::App->new->run(\@ARGV);

# vim: fdm=marker
